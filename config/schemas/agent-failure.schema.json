{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Agent failure handoff",
  "type": "object",
  "additionalProperties": false,
  "required": ["failureId", "occurrenceId", "correlationId", "failureSignature", "rootCauseFingerprint", "taskId", "agentId", "stage", "occurredAtUtc", "summary", "exitCode", "diagnostic", "evidence"],
  "properties": {
    "failureId": { "type": "string", "minLength": 1 },
    "occurrenceId": { "type": "string", "pattern": "^[a-f0-9]{32}$" },
    "correlationId": { "type": "string", "pattern": "^[A-Za-z0-9._-]{8,128}$" },
    "failureSignature": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "rootCauseFingerprint": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "taskId": { "type": "string", "minLength": 1 },
    "agentId": { "enum": ["orchestrator", "knowledge_keeper", "requirements_analyst", "developer", "reviewer", "review_verifier", "pipeline_monitor", "health_check"] },
    "stage": { "type": "string", "minLength": 1 },
    "occurredAtUtc": { "type": "string", "format": "date-time" },
    "summary": { "type": "string", "minLength": 1 },
    "exitCode": { "type": ["integer", "null"] },
    "diagnostic": { "type": ["string", "null"] },
    "evidence": { "type": "array", "items": { "type": "string", "minLength": 1 } }
  }
}
