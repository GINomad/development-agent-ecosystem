# Example project knowledge seed

Replace this placeholder with the confirmed read-only knowledge source during the guided setup interview. The installer imports only the configured extensions into the managed project knowledge root.
