interface:
  display_name: "Coordinate Delivery"
  short_description: "Orchestrate gated evidence-first delivery"
  default_prompt: "Use $coordinate-delivery to route this task through targeted analysis, implementation, review, and pipeline checks without polling or idle waits."
